# Minimal mosquitto config for the bundled docker-compose broker.
# For production, create a password file and set allow_anonymous false
# (see README "MQTT with HomeAssistant").
listener 1883 0.0.0.0
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
